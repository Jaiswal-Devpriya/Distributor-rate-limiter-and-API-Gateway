services:
  redis:
    image: redis:7
    container_name: rate-limiter-redis
    ports:
      - "6379:6379"

  gateway:
    build: .
    container_name: api-gateway
    ports:
      - "8080:8080"
    environment:
      REDIS_ADDR: redis:6379
      PORT: 8080
    depends_on:
      - redis